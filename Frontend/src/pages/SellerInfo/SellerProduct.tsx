import { Outlet } from "react-router-dom";

const SellerProduct = () => {

    return (
        <div className="seller-order-container">
            <Outlet />
        </div>
    )
}

export default SellerProduct;